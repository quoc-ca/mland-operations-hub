## 3. Configuration Management

Markdown bundles and CSV trackers are canonical in Git. Publishing to Google Workspace is guarded to `develop`; generated documents and sheets are outputs, not canonical sources.
